<nav class="navbar navbar-light bg-light border-bottom pb-3 pt-3">
  <div class="container col-md-12">
    <%= link_to "TestGuru", root_path, class: "navbar-brand" %>
    <span>
      <% if user_signed_in? %>
        <%= t('.welcome_html', email: current_user.email) %>
        <% if current_user.admin? %>
          <%= link_to 'Gists', admin_gists_path, class: 'btn btn-primary' %>
          <%= link_to 'Admin panel', admin_tests_path, class: "btn btn-primary" %>
        <% end %>
        <%= link_to t('.logout'), destroy_user_session_path, method: :delete, class: "btn btn-secondary" %>
      <% else %>
        <%= link_to t('.registration'), new_user_registration_path, class: "btn btn-primary" %>
        <%= link_to t('.login'), new_user_session_path, class: "btn btn-secondary" %>
      <% end %>
    </span>
  </div>
</nav>
