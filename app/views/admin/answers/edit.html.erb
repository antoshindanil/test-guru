<h1>Editing Answer</h1>

<p>
<%= @answer.question.body %>
</p>

<%= render 'form', answer: @answer %>

<%= link_to 'Show', admin_answer_path(@answer), class: "btn btn-secondary" %>
<%= link_to 'Back', admin_question_path(@answer.question), class: "btn btn-secondary" %>
