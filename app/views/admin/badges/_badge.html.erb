<tr>
  <td><%= badge.title %></td>
  <td><%= badge.image_path %></td>
  <td><%= badge.rule_label %></td>
  <td><%= link_to t('buttons.show'), admin_badge_path(badge) %></td>
  <td><%= link_to t('buttons.edit'), edit_admin_badge_path(badge) %></td>
  <td>
    <%= link_to t('buttons.delete'), 
                admin_badge_path(badge), 
                method: :delete, 
                data: { confirm: t('.sure') } %>
  </td>
</tr>
<%=  %>