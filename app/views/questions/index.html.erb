<h1> <%= @test.title %> </h1>

<table>
  <tr>
    <th>Question id</th>
    <th>Question body</th>
  </tr>

  <% @test.questions.each do |question| %>
    <tr>
      <td> <%= question.id %> </td>
      <td> <%= question.body %> </td>
    </tr>
  <% end %>
</table>
