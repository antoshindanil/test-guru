<% @tests.each do |test| %>
  <tr>
    <td><%= test.title %></td>
    <td><%= test_level(test) %></td>
    <td> <%= test.category.title %> </td>
    <td> <%= "#{test.author.first_name} #{test.author.last_name}"%> </td>
    <td> <%= test.questions.count %> </td>
    <td><%= button_to t('.start'), start_test_path(test), class: "btn btn-secondary" %></td>
  </tr>
<% end %>
