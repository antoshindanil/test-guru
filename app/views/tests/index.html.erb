<div class="row">
  <div class="container mt-5">
    <h1 class="mb-4"> <%= t("tests.index.header") %> </h1>
    <table class="table table-hover">
    <thead>
      <tr>
        <th><%= t('.title') %></th>
        <th><%= t('.level') %></th>
        <th><%= t('.category') %></th>
        <th><%= t('.author') %></th>
        <th><%= t('.count') %></th>
        <th><%= t('.actions') %></th>
      </tr>
      </thead>
      <tbody>
      <%= render "test" %>
      </tbody>
    </table>
  </div>
</div>
